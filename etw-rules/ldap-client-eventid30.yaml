rule:
  name: "DNS Client EventID 3006"
  provider: "Microsoft-Windows-LDAP-Client"
  task: "EventID(30)"
  opcode: "Info"
  fields:
    - ScopeOfSearch
    - SearchFilter
    - DistinguishedName
    - AttributeList
    - supportedCapabilities
